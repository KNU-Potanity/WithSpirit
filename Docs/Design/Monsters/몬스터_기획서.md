# 몬스터 기획서

## 0. 문서 목적

확장 단계에서 추가되는 몬스터의 종류, 분류, 테마별 배치, 실제 구현 상태를 정리합니다. 스탯 수치는 `스탯_기획서.md`, 스크립트 상세는 `스크립트_설명서.md`를 참고하세요.

---

## 1. 공통 규칙

- 몬스터는 닿으면 플레이어에게 데미지를 입힙니다 (하트 1 감소, `월드_스테이지_구성.md` 6장 참고).
- 정령으로 몬스터를 클릭하면 공격해 데미지를 줄 수 있습니다 (`정령_시스템_기획서.md` 2장 참고).

---

## 2. 몬스터 분류

| 분류 | 설명 |
|---|---|
| 디버프형 | 직접 데미지는 주지 않지만 접촉 시 플레이어에게 디버프 부여 |
| 근거리형 | 근접 접촉 시 데미지 |
| 원거리형 | 투사체 등으로 원거리 공격 |
| 보스 | 최종 스테이지의 보스 몬스터 |

---

## 3. 테마별 몬스터 배치

| 분류 | 몬스터 | 등장 테마 | 비고 |
|---|---|---|---|
| 근거리형 | 벌 | 테마 1 (마을·도시) | 공중형 |
| 근거리형 | 버섯 | 테마 2 (변방) | |
| 원거리형 | 스켈레톤 | 테마 2 (변방) | |
| 디버프형 | 슬라임 | 테마 2 (변방) | 접촉 시 이동 속도 감소 |
| 근거리형 | 나무 골렘 | 테마 3 (숲) | |
| 보스 | 밤 나무 | 테마 3 (숲, 최종 스테이지) | 처치가 게임 클리어 조건 |
| 근거리형 | 식충식물 | 테마 3 (숲) | 제자리 매복형 |

---

## 4. 에셋 경로

| 몬스터 | 에셋 경로 |
|---|---|
| 벌 | `Assets/100 Top Down Monsters - Pixel Art - Vol 1/Monsters/Bee` |
| 버섯 | `Assets/100 Top Down Monsters - Pixel Art - Vol 1/Monsters/Mushroom` |
| 스켈레톤 | `Assets/100 Top Down Monsters - Pixel Art - Vol 1/Monsters/Skeleton Archer` |
| 슬라임 | `Assets/100 Top Down Monsters - Pixel Art - Vol 1/Monsters/Slime` |
| 나무 골렘 | `Assets/100 Top Down Monsters - Pixel Art - Vol 1/Monsters/Plant Treant` |
| 밤 나무 | `Assets/craftpix-net-838021-top-down-pixel-ent-character-sprites/PNG/Ent3/Without_shadow` |
| 식충식물 | `Assets/craftpix-net-284465-free-predator-plant-mobs-pixel-art-pack/PNG/Plant1/Without_shadow` |

> 출처·라이선스는 `에셋_출처_라이선스_관리.md` 참고.

---

## 5. AI 이동 로직

공통 패턴: **Patrol(순찰) → Chase(추격) → Attack(공격)** 상태 머신 (스크립트 상세는 `스크립트_설명서.md` 참고).

| 상태 | 동작 |
|---|---|
| Patrol | 감지 범위 밖 — 제자리 순찰, 벽·낭떠러지 감지 시 방향 전환 |
| Chase | 감지 범위 안 + 경로 확보 — 플레이어 방향 이동 |
| Attack | 사거리 안 — 정지, 쿨타임마다 예비동작(`AttackAnimDelay`) 후 데미지 |

- 공격 판정: 좌우 방향만 적용 (정령의 몬스터 공격은 예외)
- "같은 플랫폼" 판정: 발밑 레이캐스트로 얻은 바닥 Y좌표 비교

공통 패턴과 다른 몬스터만 아래에 정리합니다 (근거리형 버섯·나무골렘은 공통 패턴 그대로라 생략).

### 5.1 공중형 (벌) — `FloatingMonsterMovement.cs`

지형 무시, `SmoothDamp`로 자유 비행. "같은 플랫폼" 판정 대신 Line of Sight로 장애물만 확인.

### 5.2 디버프형 (슬라임) — `DebuffMonsterMovement.cs`

이동 로직은 공통 패턴과 동일. 공격 명중 시 둔화 디버프만 추가로 부여 (`스탯_기획서.md` 4.1절).

### 5.3 원거리형 (스켈레톤) — `RangedMonsterMovement.cs` + `MonsterProjectile.cs`

순찰 없이 제자리 대기(Idle). 근접 대신 `FirePoint`에서 투사체를 생성해 공격 (투사체는 보스 밤송이에도 재사용되는 범용 구조).

### 5.5 식충식물 — `PlantMonsterMovement.cs`

순찰 없음, `FreezePositionX`로 좌우 이동 자체가 불가능. Idle/Attack 2상태만 존재.

### 5.6 밤송이 (보스 소환 개체) — `ChestnutMonsterMovement.cs`

사거리 안에서 포물선 점프 공격(`CalculateParabolicVelocity`)으로 돌진. 명중 시 자폭, 빗나가면 Chase로 복귀.

---

## 6. 애니메이션 구현 방식

| 애니메이션 | 구현 방식 |
|---|---|
| 멈춤/걷기 | Animator State (Idle/Walk 클립 + `Speed` 파라미터) |
| 공격 | Animator State (Attack 클립 + `Attack` 트리거) |
| 데미지 받음/죽음 | Animator가 아닌 `MonsterHealth.cs`가 처리 (피격 시 빨간색 점멸, 사망 시 알파 페이드 아웃 후 제거) |

> "지금 뭘 하는지"는 Animator가, "상태 변화 피드백"은 스크립트가 담당하도록 나눠서, 몬스터마다 Idle/Walk/Attack 클립만 갈아끼우면 재사용되도록 했습니다.

### 6.1 Animator Controller — 구현 완료

7종 전체(`AC_Monster_이름.controller`) 생성 및 Idle/Walk/Attack 클립 연결이 완료되어 있습니다. 좌우 반전은 원칙상 `MonsterFacing.cs`(모든 클립이 오른쪽만 사용) 담당이지만, 실제로는 각 이동 스크립트가 자체 처리 중이라 아직 부착되어 있지 않습니다.

### 6.2 Animator 연동 예시 — 버섯

`GroundMonsterMovement.cs`에 `Speed`/`Attack` 파라미터 반영과 공격 중 정지 로직이 적용되어 있습니다. 같은 패턴을 다른 지상 근접몹에도 동일 적용할 예정입니다.

### 6.3 스탯 데이터 — ScriptableObject 기반

`MonsterData`(공통) + `GroundMonsterData`/`FloatingMonsterData`/`RangedMonsterData`/`DebuffMonsterData`(유형별)로 구현되어 있으며, `스탯_기획서.md` 3장 수치와 필드가 대응됩니다. `ProjectileTypes`/`DebuffTypes` enum은 아직 값이 비어있습니다.

### 6.4 근접 지상 몬스터 프리팹 — 구현 완료

| 항목 | 경로 |
|---|---|
| 공통 템플릿 | `Prefabs/Entities/Monsters/GroundMonsterPrefab.prefab` |
| 버섯·나무골렘·식충식물·벌·슬라임·스켈레톤·밤송이 | 각각 `Prefabs/Entities/Monsters/{이름}.prefab` |

> 문서 확정값과 실제 에셋 값의 차이는 `QA_테스트_계획서.md` 11.2절 참고.

### 6.5 플레이어·설치 장소 프리팹화 — 구현 완료

`PlayerStartMarker.prefab`(`Prefabs/Entities/Player/`), `PlatformPlace.prefab`(`Prefabs/Terrain/`)로 정리되었습니다. 다만 기존 설치 장소 12개 중 7개는 아직 이 프리팹에 연결되지 않은 개별 오브젝트로 남아있습니다.

---

## 7. 보스(밤 나무) 패턴 설계

돌진(회피형) / 펀치(근접) / 밤송이 투척(원거리) 세 패턴으로, 정령의 서로 다른 기능(플랫폼·거리조절·공격)을 각각 파훼법으로 쓰도록 설계했습니다.

| 패턴 | 내용 | 파훼법 |
|---|---|---|
| 돌진 | 한쪽 방향으로 화면 끝까지 직선 돌진 | 플랫폼을 설치해 뛰어넘기 |
| 펀치 | 근접 시 제자리 펀치 | 거리를 벌려 회피 |
| 밤송이 투척 | 플레이어에게 날아드는 밤송이 소환 | 닿기 전 정령 공격으로 제거 |

### 7.1 설계 의도

세 패턴은 각자 독립된 쿨타임을 가지며 동시에 발동하지 않고, 조건 충족 시에도 매번 랜덤으로 발동 여부를 재판정해 패턴을 예측하기 어렵게 설계했습니다 (실제 구현은 7.5·7.6절 참고 — 다른 방식으로 구현됨).

### 7.2 공용 구조

투사체 시스템(`MonsterProjectile.cs`, 5.3절)이 스켈레톤 화살로 이미 구현되어 있어 밤송이 투척에도 그대로 재사용됩니다.

### 7.3 설계 목표 수치

| 패턴 | 목표 쿨타임 | 목표 배속/사거리 |
|---|---|---|
| 돌진 | 10초 | 평상시 이동속도의 2배 |
| 펀치 | 5초 (사거리 안일 때만) | 사거리 4 |
| 밤송이 투척 | 15초 | 3마리 동시 소환, 개체당 체력2/이속1/호밍거리1.5 |

### 7.4 설계 발동 로직

조건을 충족해도 매번 랜덤으로 발동 여부를 재판정하고, 여러 패턴이 동시에 조건을 충족하면 그중 랜덤으로 하나를 선택하도록 설계했습니다 (실제 구현은 랜덤 없이 동작 — 7.5절 참고).

### 7.5 실제 구현 현황 — 돌진·펀치 (`BossMonsterMovement.cs` + `BossMonsterData.cs`)

돌진 전 0.8초 정지(경고 연출) 후 발동하며, 벽에 부딪히거나 최대 3초 경과 시 종료됩니다. 돌진 중 경로상의 정령 플랫폼은 강제로 해제됩니다.

> 설계 목표 수치·발동 방식과의 차이는 `QA_테스트_계획서.md` 11.3절 참고.

### 7.6 실제 구현 현황 — 밤송이 투척

`BossMonsterData`에 범용 소환 패턴 리스트(`spawnPatterns`)가 추가되어, 밤송이 전용이 아닌 재사용 가능한 구조로 구현되었습니다. 소환된 밤송이는 5.6절의 독립 몬스터로 동작합니다.

> 설계 목표 소환 간격과의 차이는 `QA_테스트_계획서.md` 11.3절 참고.

---

## 8. 다음 단계

- 보스 패턴 설계(7.3·7.4)와 실제 구현(7.5·7.6) 차이 — 어느 쪽 기준으로 맞출지 결정
- 돌진의 정령 플랫폼 강제 해제가 파훼법과 충돌하는지 실제 플레이로 확인
- 밤송이 소환 간격(5초) 밸런스 확인
- 나무골렘·식충식물·밤나무·밤송이 데이터 에셋 수치를 `스탯_기획서.md` 최신 확정값으로 동기화
- `ProjectileTypes`/`DebuffTypes`/`PlatformTypes` enum 값 채우기
- `CarnivorousPlantData.prefab` 파일명 정리 (`CarnivorousPlant.prefab`으로)

> 세부 진행 상황은 `ToDoList.md`에서 관리합니다.
