# 월드/스테이지 구성 기획서

## 0. 문서 목적

> 마지막 수정일: 2026-08-26 (1.2절 마을 중앙 타일을 tile_0174에서 tile_0243으로 교체 — ver11)

확장 단계의 테마(월드)별 스테이지 구성, 정령 등장 진행, 스토리 개요, 클리어·게임오버 조건을 정리합니다.

---

## 1. 테마(월드) 구성

| 테마 | 배경 | 스테이지 수 | 정령 등장 |
|---|---|---|---|
| 테마 1 | 마을 또는 도시 | 1 스테이지 (약 5분 분량, 튜토리얼 격) | 기본 정령만 등장 |
| 테마 2 | 변방 | 2 스테이지 | 추가 정령 1~2개 등장 |
| 테마 3 | 숲 | 2 스테이지 | 추가 정령 최대 개수(3개)까지 등장 |

> 기본 테마는 총 3개입니다.

### 1.2 테마별 배경/타일 에셋 (2026-08-20 추가, 2026-08-25 타일 9종 배치 순서 반영)

| 테마 | 종류 | 파일 | 위치 |
|---|---|---|---|
| 테마 1 (마을) | 배경 | `tile_0247.png` | `Assets/craftpix-896753-village-pixel-art-environment-assets-pack/PNG/Tiles/` |
| 테마 3 (숲) | 배경 | `Background.png` | `Assets/craftpix-net-247157-tiny-forest-tileset-platformer-pixel-art/1 Background/` |

**타일 9종 배치 순서 (3×3 오토타일 그리드)**

타일 9개는 아무렇게나 나열한 게 아니라, **주변에 어떤 타일이 있는지에 따라 끼워 넣을 타일이 달라지는 3×3 배치 규칙**을 따릅니다. 아래처럼 3×3 칸에 번호를 매겨서, 각 칸 위치에 어떤 타일 파일이 들어가는지로 정리합니다.

```
[1][2][3]   ← 윗줄 (좌상단 모서리 / 상단 가장자리 / 우상단 모서리)
[4][5][6]   ← 가운데줄 (좌측 가장자리 / 중앙 채움 / 우측 가장자리)
[7][8][9]   ← 아랫줄 (좌하단 모서리 / 하단 가장자리 / 우하단 모서리)
```

| 칸 번호 | 위치 의미 | 테마 1 (마을) 파일 | 테마 3 (숲) 파일 |
|---|---|---|---|
| 1 | 좌상단 모서리 | `tile_0054.png` | `Jungle_tile_01.png` |
| 2 | 상단 가장자리 | `tile_0140.png` | `Jungle_tile_02.png` |
| 3 | 우상단 모서리 | `tile_0042.png` | `Jungle_tile_03.png` |
| 4 | 좌측 가장자리 | `tile_0021.png` | `Jungle_tile_10.png` |
| 5 | 중앙 채움 | `tile_0243.png` (2026-08-26 수정 — 협의 결과 `tile_0174`보다 자연스러워서 교체) | `Jungle_tile_11.png` |
| 6 | 우측 가장자리 | `tile_0017.png` | `Jungle_tile_12.png` |
| 7 | 좌하단 모서리 | `tile_0022.png` | `Jungle_tile_19.png` |
| 8 | 하단 가장자리 | `tile_0043.png` | `Jungle_tile_20.png` |
| 9 | 우하단 모서리 | `tile_0020.png` | `Jungle_tile_21.png` |

> 두 테마 모두 `Assets/craftpix-.../` 원본 폴더 경로는 위 표(배경) 기준과 동일 폴더(`.../PNG/Tiles/`, `.../2 Tiles/`)에 있습니다.

> 테마 2(변방)의 배경/타일 에셋은 아직 지정되지 않았습니다.

---

### 1.1 테마 1 상세 (튜토리얼)

1 스테이지를 두 파트로 구성합니다.

| 파트 | 내용 |
|---|---|
| 앞 파트 | 정령 없이 진행. 조작키(이동·점프) 튜토리얼. 중간에 플레이어 혼자서는 통과할 수 없는 구간이 등장 |
| 전환 | 위 구간에서 정령이 등장해 문제를 해결 → 후반부로 진입 |
| 뒤 파트 | 정령 사용법(마커, 클릭 상호작용) 튜토리얼 |

---

## 2. 스토리

스토리 구체화 작업이 완료되어, 상세 내용은 별도 문서로 분리했습니다.

- **상세 내용**: `Docs/Design/Story/스토리.md` 참고 (로그라인, 테마별 스토리 비트, 정령·보스의 서사적 의미, 엔딩 개요 포함)
- 정령을 동료(자아)로 설정한 세계관(`정령_시스템_기획서.md` 4장)과 맞물리도록 작성되었습니다.

---

## 3. 스테이지 구성 요소

스테이지는 아래 요소들로 구성됩니다.

- 플랫폼 (일반·이동·무너짐·스탯변화 — `발판_확장_기획서.md`)
- 장애물 (가시, 낙사 등 기존 MVP 요소)
- 뒷배경
- 장식
- 몬스터 (`몬스터_기획서.md`)
- 설치 장소 (정령이 플랫폼을 생성할 수 있는 지점)

### 3.1 맵 배치 순서 (2026-08-25 추가)

스테이지를 실제로 배치할 때는 아래 순서로 쌓습니다.

1. 플랫폼
2. 배경
3. 상호작용 가능한 타일 (설치 장소 등)
4. 몬스터
5. 보스
6. 장식

---

## 4. 카메라

확장 단계에서도 카메라 로직은 수정하지 않습니다. 기존 추적 카메라(`기술기획서.md` 4.5절)를 그대로 사용합니다.

---

## 5. 게임 클리어 기준

| 단위 | 조건 |
|---|---|
| 스테이지 | 각 스테이지의 도착 지점에 도달하면 클리어 |
| 게임 전체 | 최종 스테이지(테마 3)에서 보스(밤 나무) 처치 |

---

## 6. 리스폰 / 생명

- 리스폰 방식은 MVP와 동일하게 즉시 시작 지점으로 복귀합니다 (체크포인트 없음).
- 플레이어 체력은 하트 아이콘으로 표시되며, 최대 개수는 **4개**입니다.
  - 낙사: 체력이 즉시 0으로 감소 (하트 전부 소진 → 8장 게임 오버 조건 트리거)
  - 낙사를 제외한 데미지(몬스터 접촉, 원거리 공격, 가시 등): 하트 1씩 감소
  - 하트가 모두 소진되면 게임 오버 (8장 참고)

### 6.1 하트 UI

| 항목 | 내용 |
|---|---|
| 최대 개수 | 4개 |
| 위치 | 화면 좌측 상단 |
| 배치 방식 | 왼쪽부터 오른쪽으로 4개를 가로로 나열, 현재 체력만큼 채워진 하트로, 나머지는 빈 하트로 표시 |
| 채워진 하트 (체력 있음) | 빨간색 채우기 + 검정색 외곽선 |
| 빈 하트 (체력 없음) | 검정색 단색 (채우기 없이 검정 아이콘) |

**구현 방식 (2026-07-16, 실제 코드 기준으로 수정)**: `HeartUI.cs`는 색 틴트가 아니라 **하트 개당 스프라이트를 통째로 교체하는 방식**으로 구현되어 있습니다. 현재 체력보다 인덱스가 작은 하트는 `fullHeartSprite`로, 그 이상은 `emptyHeartSprite`로 교체합니다.

| 스프라이트 필드 | 실제 연결된 에셋 (2026-07-21 확인) | 비고 |
|---|---|---|
| `fullHeartSprite` | `Assets/_Project/Art/UI/ui_heart_full.png` | Unity 자체 기능으로 직접 만든 **임시 플레이스홀더**. 곧 정식 아트로 교체 예정 |
| `emptyHeartSprite` | `Assets/_Project/Art/UI/ui_heart_empty.png` | 위와 동일하게 임시 플레이스홀더 |

- 이전에 검토했던 "외곽선(`heart.png`) + 채우기(`heart_fill.png`) 2-레이어를 `color`로 틴트"하는 방식과, 그 이후의 "Flaticon 기반 완성 이미지 하나 제작" 방향 모두 **실제로는 채택되지 않았습니다.** 지금은 Unity 자체 기능으로 만든 임시 스프라이트 2장이 연결되어 있으며, 정식 아트가 나오면 교체될 예정입니다.
- 제작 시 참고했던 `Assets/UI/Icons/Heart/heart.png`, `heart_fill.png`(Flaticon 기반)는 현재 **어디에도 연결되어 있지 않은 미사용 파일**입니다. 정식 아트 제작 시 참고용으로만 남겨두거나 정리가 필요합니다.
- 정식 아트로 교체될 때까지는 출처 표기 의무가 없습니다 (임시 플레이스홀더는 Unity 자체 기능으로 직접 제작). 다만 최종적으로 Flaticon 기반 이미지를 쓰기로 결정되면 그때 다시 `에셋_출처_라이선스_관리.md`에 표기해야 합니다.

---

## 7. 게임 오버 요소 (데미지·사망 원인)

| 요소 | 설명 |
|---|---|
| 가시 | 기존 MVP 즉사 장애물 |
| 낙사 | 체력이 즉시 0으로 감소 (하트 전부 소진, 게임 오버 트리거) — 즉시 리스폰 없이 8장 게임 오버 조건으로 처리 |
| 원거리 공격 | 몬스터의 투사체 등에 맞음 |
| 접촉 데미지 | 몬스터에 직접 닿음 |
| 디버프 | 슬라임 등 디버프형 몬스터 접촉 |

---

## 8. 게임 엔딩 조건

- 승리: 최종 보스(밤 나무) 처치
- 패배: 하트를 모두 소진 (구체적인 패널티·화면 구성은 추후 확정)

---

## 9. 재화

코인·젬 등 재화 시스템은 도입하지 않습니다 (`플랫포머_게임_기획서_확장기능.md`의 기존 수집·보상 후보 항목 폐기).

---

## 10. 메인 메뉴 및 설정 UI (2026-08-25 추가)

### 10.1 시작 화면

세로로 나열된 버튼 3개로 구성됩니다.

| 버튼 | 기능 |
|---|---|
| 게임 시작 | 게임 플레이 시작 |
| 설정 | 설정 화면(10.2·10.3절)으로 이동 |
| 게임 종료 | 게임 종료 |

### 10.2 설정 — 사운드 화면

상단에 뒤로가기(←) 버튼과 탭 2개(**사운드** / **조작 방법**)가 있고, 사운드 탭이 기본 선택된 상태입니다.

| 항목 | 내용 |
|---|---|
| 배경 음악 | 슬라이더, 기본값 80% |
| 효과음 | 슬라이더, 기본값 80% |

### 10.3 설정 — 조작 방법 화면

같은 설정 화면 안에서 상단 탭을 **조작 방법**으로 전환하면 나옵니다. 좌우 두 열(행동 / 조작키)로 구성됩니다.

| 행동 | 조작키 |
|---|---|
| 왼쪽 이동 | a |
| 오른쪽 이동 | d |
| 점프 | w / space |
| 상호작용 | 마우스 좌클릭 |
| 정령 전환 | 마우스 우클릭 |

> 조작키 내용은 `캐릭터컨트롤_스펙.md`에 이미 정의된 값과 동일합니다 (UI 안내 목적으로 이 화면에 표시).

---

## 11. 다음 단계

- 게임오버 시 UI·화면 구성 확정
- 보스(밤 나무) 패턴은 별도 설계 예정
